// Superseded in v1.9 by projects2.test.ts (async ProjectStore).
// Kept as a placeholder so old scripts referencing this file still pass.
import { test } from "node:test";
import assert from "node:assert/strict";

test("see projects2.test.ts for the async store tests", () => {
  assert.ok(true);
});
